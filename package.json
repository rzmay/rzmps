{
  "name": "RZMPS Monorepo",
  "version": "1.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -k \"npm run dev --workspace @rzmps/rzmps\" \"npm start --workspace demo\""
  },
  "devDependencies": {
    "tsdown": "^0.23.0"
  }
}
